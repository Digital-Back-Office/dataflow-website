---
import { getEntry } from 'astro:content'
import EmailCTA from '../common/EmailCTA.astro'
import BackgroundAnimatedNodes from './BackgroundAnimatedNodes.astro'

const Data = await getEntry('home', 'home')
const hero = Data.data?.hero || {
  title: '',
  description: '',
  subtitle: '',
  primary_cta: { text: '', href: '' },
  secondary_cta: { text: '', href: '' },
  cta_button: '',
  badges: [],
  supported_by: []
}
---

<section
  id="hero-section"
  class="relative px-[5%] pt-32 pb-10 max-sm:pt-24 max-sm:pb-16 flex flex-col items-center max-sm:mt-10"
  
>
  <!-- Background Pattern with Dataflow Animation -->
  <BackgroundAnimatedNodes />

  <div class="max-w-7xl mx-auto relative z-10 text-center">
    <div class="mb-12">
      <h1
        class="text-6xl max-sm:text-4xl font-bold text-gray-900 mb-6 leading-tight"
        data-aos="fade-up"
      >
        {(hero?.title || 'Focus on Data Logic, <br> Not Glue Code.')
          .split(/<br\s*\/?>/i)
          .map((line, i, arr) => (
            <span>
              {line}
              {i !== arr.length - 1 && <br />}
            </span>
          ))}
      </h1>
      <p
        class="text-xl max-sm:text-lg text-gray-600 mb-8 max-w-3xl mx-auto"
        data-aos="fade-up"
        data-aos-delay="100"
      >
        {
          (hero?.description || 'All your data tools, finally working together—without breaking environments. <br/> AI ready and Open source tools.')
            .split(/<br\s*\/?>/i)
            .map((line, i, arr) => (
              <span>
                {line}
                {i !== arr.length - 1 && <br />}
              </span>
            ))
        }</p>
      <!-- Email CTA Form -->
      <div data-aos="fade-up" data-aos-delay="200" class="mb-8 w-full">
        <EmailCTA formId="heroEmailForm" pageSource="homepage-hero" />
      </div>
      
      <div
        class="flex items-center justify-center gap-8 text-sm text-gray-600 max-sm:flex-col max-sm:gap-4"
        data-aos="fade-up"
        data-aos-delay="300"
      >
        {
          (hero.badges || []).map((label) => (
            <div
              class="flex items-center gap-2"
              data-aos="fade-up"
              data-aos-delay="350"
            >
              <svg
                class="w-5 h-5 text-primary"
                fill="currentColor"
                viewBox="0 0 20 20"
              >
                <path
                  fill-rule="evenodd"
                  d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                  clip-rule="evenodd"
                />
              </svg>
              <span>{label}</span>
            </div>
          ))
        }
      </div>
    </div>

    
    <!-- Platform Demo Video -->
    <div 
      class="relative w-full max-w-6xl mx-auto mt-16"
      data-aos="fade-up"
      data-aos-delay="400"
    >
      <div 
        class="relative rounded-2xl overflow-hidden shadow-xl"
      >
        <video
          id="heroVideo"
          autoplay
          loop
          muted
          playsinline
          class="w-full h-auto block"
          poster="/assets/images/app-screenshots/dashboard.webp"
        >
          <!-- WebM format for better compression (preferred) -->
          <source src="/assets/videos/hero-video-v2.webm" type="video/webm" />
          <!-- Fallback to image if video doesn't load -->
          <img 
            src="/assets/images/app-screenshots/dashboard.webp" 
            alt="Dataflow Platform Dashboard" 
            class="w-full h-auto"
          />
        </video>
        <!-- Unmute Button -->
        <button
          id="unmuteBtn"
          class="absolute bottom-4 right-4 p-3 bg-white/90 hover:bg-white rounded-full shadow-lg transition-all duration-300 hover:scale-110 focus:outline-none focus:ring-2 focus:ring-primary/50"
          aria-label="Toggle sound"
        >
          <svg id="mutedIcon" class="w-5 h-5 text-gray-700" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M9.383 3.076A1 1 0 0110 4v12a1 1 0 01-1.707.707L4.586 13H2a1 1 0 01-1-1V8a1 1 0 011-1h2.586l3.707-3.707a1 1 0 011.09-.217zM12.293 7.293a1 1 0 011.414 0L15 8.586l1.293-1.293a1 1 0 111.414 1.414L16.414 10l1.293 1.293a1 1 0 01-1.414 1.414L15 11.414l-1.293 1.293a1 1 0 01-1.414-1.414L13.586 10l-1.293-1.293a1 1 0 010-1.414z" clip-rule="evenodd"/>
          </svg>
          <svg id="unmutedIcon" class="w-5 h-5 text-gray-700 hidden" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M9.383 3.076A1 1 0 0110 4v12a1 1 0 01-1.707.707L4.586 13H2a1 1 0 01-1-1V8a1 1 0 011-1h2.586l3.707-3.707a1 1 0 011.09-.217zM14.657 2.929a1 1 0 011.414 0A9.972 9.972 0 0119 10a9.972 9.972 0 01-2.929 7.071 1 1 0 01-1.414-1.414A7.971 7.971 0 0017 10c0-2.21-.894-4.208-2.343-5.657a1 1 0 010-1.414zm-2.829 2.828a1 1 0 011.415 0A5.983 5.983 0 0115 10a5.984 5.984 0 01-1.757 4.243 1 1 0 01-1.415-1.415A3.984 3.984 0 0013 10a3.983 3.983 0 00-1.172-2.828 1 1 0 010-1.415z" clip-rule="evenodd"/>
          </svg>
        </button>
        <!-- Subtle overlay gradient -->
        <div class="absolute inset-0 bg-gradient-to-t from-black/5 to-transparent pointer-events-none"></div>
      </div>
    </div>

    <script is:inline>
      // Handle video unmute
      const video = document.getElementById('heroVideo');
      const unmuteBtn = document.getElementById('unmuteBtn');
      const mutedIcon = document.getElementById('mutedIcon');
      const unmutedIcon = document.getElementById('unmutedIcon');
      
      if (video && unmuteBtn && mutedIcon && unmutedIcon) {
        unmuteBtn.addEventListener('click', () => {
          if (video.muted) {
            video.muted = false;
            mutedIcon.classList.add('hidden');
            unmutedIcon.classList.remove('hidden');
          } else {
            video.muted = true;
            mutedIcon.classList.remove('hidden');
            unmutedIcon.classList.add('hidden');
          }
        });
      }
    </script>

    <!-- Powered By Section -->
     <div 
      class="mt-20 w-full flex flex-col items-center justify-center"
      data-aos="fade-up"
      data-aos-delay="500"
    >
      <p 
        class="text-center text-sm text-gray-500 mb-6 tracking-wider"
        data-aos="fade-up"
        data-aos-delay="600"
      >
        Powered by
      </p>
      <div class="flex items-center gap-10 max-sm:gap-6 flex-wrap justify-center">
        {
          (hero.supported_by || []).map((partner, index) => (
            <img
              src={partner.logo}
              alt={partner.name}
              loading="lazy"
              class="h-[25px] max-sm:h-[20px] transition-all duration-300 opacity-60 hover:opacity-100"
              data-aos="zoom-in"
              data-aos-delay={500 + index * 100}
            />
          ))
        }
      </div>
  </div>
</section>